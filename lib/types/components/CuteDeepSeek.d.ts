import React from 'react';
/** Render the source-aligned grid as 17 ANSI rows using half-block glyphs. */
export declare function renderCuteDeepSeekRows(pulse?: number): string[];
/** The CuteDeepSeek mascot, pinned beside the header text. */
export declare function CuteDeepSeekArt({ pulse, width }: {
    pulse?: number;
    width?: number;
}): React.ReactNode;
//# sourceMappingURL=CuteDeepSeek.d.ts.map